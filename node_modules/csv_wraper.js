//--------------------------------------------------------------------------------------------------------
// A csv module wrapper
//--------------------------------------------------------------------------------------------------------
csv = require('csv')

exports.open = function(path) {
    var ret = []

    csv()
        .fromPath(path)
        .on('data', function(data, index) {
            ret[index] = data
        })
        .on('end',function(count){
            
        })
}